{   
    "userId": {
        "type": "string"
    },

    "userAI2": {
        "type": "string"
    },

    "contextEQ": {
        "type": "string"
    },

    "templateName": {
        "type": "string"
    },

    "taxOfficeName": {
        "type": "string"
    },

    "taxOfficeAddress": {
        "type": "string"
    },

    "identificationType": {
        "type": "string",
        "enum": ["ID", "PESEL", "REGON", "NIP", "PASSPORT", "OTHER"]
    },

    "identificationNumber": {
        "type": "string"
    },

    "operationAmount": {
        "type": "string"
    },

    "operationCurrency": {
        "type": "string"
    },

    "statusAuth": {
        "type": "string",
        "enum": ["null", "SUCCESS", "FAILURE"]
    },

    "status": {
        "type": "string",
        "enum": ["ATTEMPT", "SUCCESS", "FAILURE", "TIMEOUT"]
    }
}