{   
    "userId": {
        "type": "string"
    },

    "userAI2": {
        "type": "string"
    },

    "contextEQ": {
        "type": "string"
    },

    "billingCycle": {
        "type": "string"
    },

    "newBillingCycle": {
        "type": "string"
    },

    "cardType": {
        "type": "string"
    },

    "cardNo": {
        "type": "string"
    },

    "statusAuth": {
        "type": "string",
        "enum": ["null", "SUCCESS", "FAILURE"]
    },

    "status": {
        "type": "string",
        "enum": ["ATTEMPT", "SUCCESS", "FAILURE", "TIMEOUT"]
    }
}