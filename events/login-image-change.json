{   
    "login": {
        "type": "string"
    },

    "userId": {
        "type": "string"
    },

    "userAI2": {
        "type": "string"
    },

    "imageType": {
        "type": "string",
        "enum": ["IMAGE", "INITIALS"]
    },

    "actionType": {
        "type": "string",
        "enum": ["ADD", "CHANGE", "REMOVE"]
    },

    "statusAuth": {
        "type": "string",
        "enum": ["null", "SUCCESS", "FAILURE"]
    },

    "status": {
        "type": "string",
        "enum": ["ATTEMPT", "SUCCESS", "FAILURE", "TIMEOUT"]
    }
} 