{   
    "userId": {
        "type": "string"
    },

    "userAI2": {
        "type": "string"
    },

    "phone": {
        "type": "string"
    },

    "pesel": {
        "type": "string"
    },

    "login": {
        "type": "string"
    },

    "status": {
        "type": "string",
        "enum": ["SUCCESS", "FAILURE", "TIMEOUT"]
    }
}