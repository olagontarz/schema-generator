{   
    "login": {
        "type": "string"
    },

    "userId": {
        "type": "string"
    },

    "userAI2": {
        "type": "string"
    },

    "newAuthorizationMethod": {
        "type": "string"
    },

    "statusAuth": {
        "type": "string",
        "enum": ["null", "SUCCESS", "FAILURE"]
    },

    "status": {
        "type": "string",
        "enum": ["ATTEMPT", "SUCCESS", "FAILURE", "TIMEOUT"]
    }
} 