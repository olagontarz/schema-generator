{   
    "userId": {
        "type": "string"
    },

    "userAI2": {
        "type": "string"
    },

    "login": {
        "type": "string"
    },

    "hashMethod": {
        "type": "string"
    },

    "status": {
        "type": "string",
        "enum": ["SUCCESS", "FAILURE", "TIMEOUT"]
    },

    "operatorId": {
        "type": "string"
    },

    "pinType": {
        "type": "string",
        "enum": ["CLIENT", "RANDOM"]
    },

    "smsPhoneNumber": {
        "type": "string"
    }
}