{   
    "userId": {
        "type": "string"
    },

    "userAI2": {
        "type": "string"
    },

    "channel": {
        "type": "string"
    },

    "blockadeReason": {
        "type": "string"
    },

    "maxAttempts": {
        "type": "string"
    },

    "status": {
        "type": "string",
        "enum": ["SUCCESS", "FAILURE", "TIMEOUT"]
    }
}