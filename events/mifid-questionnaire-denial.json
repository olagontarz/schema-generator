{   
    "userId": {
        "type": "string"
    },

    "userAI2": {
        "type": "string"
    },

    "category": {
        "type": "string"
    },

    "email": {
        "type": "string"
    },

    "status": {
        "type": "string",
        "enum": ["ATTEMPT", "SUCCESS", "FAILURE", "TIMEOUT"]
    }
}