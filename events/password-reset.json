{ 
    "os": {
        "type": "string"
    },

    "graphics": {
        "type": "string"
    },

    "resolution": {
        "type": "string"
    },

    "colorDepth": {
        "type": "string"
    },

    "browserLanguage": {
        "type": "string"
    },
            
    "acceptLanguage": {
        "type": "string"
    },

    "timeZone": {
        "type": "string"
    },

    "extensions":  {
        "type": "string"
    },

    "platform": {
        "type": "string"
    },

    "osLanguage": {
        "type": "string"
    },

    "login": {
        "type": "string"
    },

    "userId": {
        "type": "string"
    },

    "userAI2": {
        "type": "string"
    },

    "processStep": {
        "type": "string",
        "enum": ["START", "OTP-ENTER", "DEFINE-METHOD", "SET-PASSWORD"]
    },

    "statusAuth": {
        "type": "string",
        "enum": ["null", "SUCCESS", "FAILURE"]
    },

    "status": {
        "type": "string",
        "enum": ["ATTEMPT", "SUCCESS", "FAILURE", "TIMEOUT"]
    }
}