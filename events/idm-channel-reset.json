{   
    "userId": {
        "type": "string"
    },

    "userAI2": {
        "type": "string"
    },

    "affectedChannel": {
        "type": "string",
        "enum": ["INTERNET", "MOBILE", "IVR"]
    },

    "reason": {
        "type": "string"
    },

    "status": {
        "type": "string",
        "enum": ["SUCCESS", "FAILURE", "TIMEOUT"]
    },

    "operatorId": {
        "type": "string"
    }
}
