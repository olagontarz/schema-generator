{   
    "userId": {
        "type": "string"
    },

    "userAI2": {
        "type": "string"
    },

    "sidomaId": {
        "type": "string"
    },

    "status": {
        "type": "string",
        "enum": ["ATTEMPT", "SUCCESS", "FAILURE", "TIMEOUT"]
    }
}
