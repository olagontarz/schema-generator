{   
    "userId": {
        "type": "string"
    },

    "userAI2": {
        "type": "string"
    },

    "status": {
        "type": "string",
        "enum": ["ATTEMPT", "SUCCESS", "FAILURE", "TIMEOUT"]
    }
}
