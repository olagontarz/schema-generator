{   
    "status2FA": {
        "type": "string",
        "enum": ["OTP", "TOKEN", "DISABLED"]
    },

    "login": {
        "type": "string"
    },

    "userId": {
        "type": "string"
    },

    "userAI2": {
        "type": "string"
    },

    "statusAuth": {
        "type": "string",
        "enum": ["null", "SUCCESS", "FAILURE"]
    },

    "status": {
        "type": "string",
        "enum": ["ATTEMPT", "SUCCESS", "FAILURE", "TIMEOUT"]
    }
}